============
Installation
============

At the command line::

    $ easy_install dj-starboard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-starboard
    $ pip install dj-starboard
